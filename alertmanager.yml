route:

  group_by: ['job']

  group_wait: 0s

  group_interval: 1s

  repeat_interval: 5s

  receiver: webhook

  routes:
  - receiver: webhook
    continue: true  
receivers:
    - name: webhook
      webhook_configs:
      - url: 'http://192.168.1.205:9000/alert'
        # send_resolved: true